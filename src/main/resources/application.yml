
spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/asd
    username: sepauser
    password: password
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect